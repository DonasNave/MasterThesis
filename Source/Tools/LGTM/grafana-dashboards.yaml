apiVersion: 1

providers:
  - name: 'Domain metrics'
    type: file
    options:
      path: /otel-lgtm/domain-metrics.json # place into root no problem
      foldersFromFilesStructure: false

  - name: 'Performance metrics'
    type: file
    options:
      path: /otel-lgtm/performance-data.json # place into root no problem
      foldersFromFilesStructure: false
