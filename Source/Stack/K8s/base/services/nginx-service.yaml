apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  type: LoadBalancer # Or NodePort, depending on your environment
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: nginx
