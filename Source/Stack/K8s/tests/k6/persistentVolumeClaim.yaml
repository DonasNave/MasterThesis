apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: scenarios-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # When using dynamic provisioning, use storageClassName.
  # storageClassName: my-storage-class
